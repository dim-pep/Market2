Client логинется
|
sessionservice выдаёт ему access и refresh_token
Создаётся запись access__token и session_id и user_id
Client в ответ получает session_id и user_id
|
Запрос на создание ордера происходит с user_id. С помощью user_id в userservice узнаём роли сравниваем с теми что допустимы в этом Market если проходит создаём ордер
|
Отвечаем что маркет создался нормально

Регистрация отдельно
Нельзя залогинется пока не зарегистрирован

Client регистрируется
|
Пароль,username
Пароль надо зашифровать
|
Сохраняем в БД

Client c переодичностью жизни access__token обновляет оба токена

При запросе на создание оредера проверяем access__token совпадает ли он с session_id и не протух ли он

